import React from 'react';
import {Box} from "@mui/system";
import {ColorsBackgroundPrimaryBackground} from "datami-ui-kit/dist/esm/style-dictionary-dist/tokens";

const SideBar = () => {
    return (
        <Box sx={{backgroundColor: ColorsBackgroundPrimaryBackground, flex: 1}}>
        </Box>
    );
};

export default SideBar;
